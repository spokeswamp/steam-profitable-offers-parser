config = {
    'debug': False,  # False by default. to use a test file with items
    'cooldown_time': 3,  # 3 by default. in seconds, for steam market api
    'usd_to_custom': 1,  # 1 by default. If you use a currency other than the dollar - this must be changed

    'cookies': {},  # {} by default. Change this if you have problems with the API
}
